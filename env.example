# Next.js
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
NEXT_PUBLIC_DEFAULT_USER_ID=

# Image provider (Replicate only)
MAMETTE_IMAGE_PROVIDER=replicate

# Replicate configuration
REPLICATE_API_TOKEN=
# Provide either a model slug (owner/model) to auto-resolve latest version,
# or set a specific version id. Example model slug below can be replaced.
REPLICATE_MODEL=bananaml/nano-nano
REPLICATE_VERSION=

# OCR toggle (disable for speed or if worker issues)
MAMETTE_OCR_ENABLED=true 